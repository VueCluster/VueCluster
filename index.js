var EventEmitter = require('events').EventEmitter

var VueCluster = function(options) {
	var self = this
}

VueCluster.prototype = Object.create(EventEmitter.prototype)

module.exports.VueCluster = VueCluster